#include <bits/stdc++.h>

using namespace std;

int suma(int a, int b){
	return a+b;
}

void hacer_algo(){
	//hacemos cosas
	//no necesitamos retornar algo
}

int main(){
	// cin viene de console in
	// cout viene de console out
	/*

  // While
	int i = 0;
	while(i < 10){
		if(i%2 == 0){
			cout << i << endl;
		}
		i++;
	}

	//for(inicializacion; condicion; finalizacion_ciclo)
	for(int i = 0; i < 10; i++){
		cout << i << endl;
	}
	
  // Arreglos
	int a[] = {10, 15, 20, 1, 5};
	cout << a[2] << endl;
	
	// Ejemplo uso de vectores
	int n = 100;
	vector <int> mi_vector(n, 1); // Inicializamos un vector de n unos
	mi_vector.push_back(500); // Agregamos un 500 al final (como append de Python)
	for(int x : mi_vector){ // for al estilo Python (sólo C++11 en adelante)
		cout << x << endl;
	}
	
	// Ejemplo de leer un arreglo de n elementos
	int n;
	cin >> n;
	vector <int> arr(n);
	for(int i=0; i<n; i++){
		cin >> arr[i];
	}
	
  // Strings
	string a = "acababa";
	char b = '9';
	a[5];
  */
	return 0;
}
